import styled from "styled-components";

export const Return = styled.div`
    display: flex;
    flex-direction: row;
    align-items: center;
    font-family: var(--boundedFont);
    color: #DCD1D1;
    position: relative;
    top: 40px;
    left: 20px;

    position: fixed;
`;